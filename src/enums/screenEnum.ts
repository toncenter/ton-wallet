export enum ScreenEnum {
    start = 'start',
    created = 'created',
    backup = 'backup',
    createPassword = 'createPassword',
    readyToGo = 'readyToGo',
    import = 'import',
    main = 'main',
}
